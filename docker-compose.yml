version: '3'

services:
  website:
    build:
      context: .
      dockerfile: Dockerfile
  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    environment:
      - DOCKER_COMPOSE=true
    depends_on:
      - website


